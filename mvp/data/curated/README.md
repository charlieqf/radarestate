# Curated Data

放可直接给报告、图表、清单使用的主题表。

建议优先产出的主题表：

- `precincts`
- `policy_signals`
- `application_signals`
- `constraints`
- `opportunity_items`
- `watchlist_scoreboard`

原则：

- 一张表只服务少数明确用途
- 可直接被周报、地图和 Top N 清单消费
- 尽量附带 `source_url` 和 `last_reviewed_at`
